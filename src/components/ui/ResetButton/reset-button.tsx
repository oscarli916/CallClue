import { Button } from "@mui/material";

const ResetButton = () => {
	return (
		<Button
			style={{
				backgroundColor: "white",
				color: "#4b48ca",
			}}
			variant="contained"
		>
			Reset
		</Button>
	);
};

export default ResetButton;
