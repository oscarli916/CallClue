import { Button } from "@mui/material";

const SaveButton = () => {
	return (
		<Button
			style={{
				backgroundColor: "#4b48ca",
				color: "white",
			}}
			variant="contained"
		>
			Save
		</Button>
	);
};

export default SaveButton;
